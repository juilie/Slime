export const shaders = {
    vertex: `/* vert.glsl content */`,
    original: {
      sim: `/* original/sim.glsl content */`,
      render: `/* original/render.glsl content */`,
      config: {/* config object */}
    },
    glass: {
      // ... similar structure
    }
  };